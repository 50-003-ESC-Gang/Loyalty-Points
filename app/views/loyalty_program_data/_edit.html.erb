<%= stylesheet_link_tag "login" %>


<%= form_with(model: loyalty_program_datum) do |form| %>
  <% if loyalty_program_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(loyalty_program_datum.errors.count, "error") %> prohibited this loyalty_program_datum from being saved:</h2>

      <ul>
        <% loyalty_program_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">
    <%= form.label :points, style: "display: block", class: "label" %>
    <%= form.text_field :in_points, class: "input", placeholder: "ex. 100"%>
    <%= form.submit "Transfer", class: "button"%>
  </div>
  <br>


    

<% end %>